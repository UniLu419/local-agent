local-agent
===========

Simple LangGraph-based local agent project.

Usage
-----

- Install dependencies and the project:

```bash
poetry install
```

- Run the agent entrypoint:

```bash
poetry run agent
```

